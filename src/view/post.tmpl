{% function block.content() %}
  <nav>
    {% if has_user_access then %}
      <a href="/{%& username %}/{%& slug %}/edit">Edit</a>
    {% end %}
    <a href="/{%& username %}">Back</a>
  </nav>

  <main>
    {%= content %}
  </main>
{% end %}

{% render('layout') %}